#include <stdio.h>
//#include "019_02_static_and_global_test_helper.c"
int main(void)
{

    extern char a; // extern variable must be declared before use
     printf("%c ", a);
     (void)msg();
     return 0;
}
